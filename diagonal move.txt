  for(int  t = 0; t<aa+bb; ++t)
    {
        ll sum=0,sum2=0;
        multiset<ll>s1,s2;
        for(int  i=t, j=0; i>=0 ; --i, ++j)
        {
            if( (i<aa) && (j<bb) )

            {

                s1.insert(a[i][j]);
                s2.insert(b[i][j]);

                
            }
        }